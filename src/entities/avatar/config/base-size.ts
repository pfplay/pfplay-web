/**
 * face x, y좌표의 기준이되는 body의 높이와 너비.
 */
export const BODY_BASE_WIDTH = 120;
export const BODY_BASE_HEIGHT = 160;

/**
 * face 중앙점을 (0,0) 기준으로 좌(-), 우(+) 이동 얼굴 너비 대비 비율
 */
export const BASE_X = 0;
/**
 * face 중앙점을 (0,0) 기준으로 위(-), 아래(+) 이동 얼굴 높이 대비 비율
 */
export const BASE_Y = 0;
/**
 * face 크기 조절 배율
 */
export const BASE_SCALE = 1;

/**
 * BODY_BASE_WIDTH, BODY_BASE_HEIGHT에 따른 FACE 너비와 높이 비율
 */
export const FACE_BASE_WIDTH_RATIO = /* 32 / 120 */ 0.26666666666666666;
export const FACE_BASE_HEIGHT_RATIO = /* 36/ 160 */ 0.225;
